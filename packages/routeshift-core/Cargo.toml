[package]
name = "routeshift-core"
version = "0.1.0"
edition = "2021"
description = "Core traffic assignment algorithms for RouteShift"

[dependencies]
serde = { version = "1", features = ["derive"] }
